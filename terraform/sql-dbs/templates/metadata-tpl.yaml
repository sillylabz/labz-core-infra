network:
  version: 2
  ethernets:
    ens192:
      dhcp4: false
      addresses: 
        - ${vm_ip4_address}
      gateway4: ${vm_ip4_gateway}
      nameservers:
        addresses: 
          - ${vm_dns_server}
local-hostname: ${vm_hostname}
instance-id: ${vm_hostname}
